import NoteEditorUser from 'App/Models/NoteEditorUser'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(NoteEditorUser, ({ faker }) => {
  return {
    //
  }
}).build()
