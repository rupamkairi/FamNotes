import NoteViewerUsers from 'App/Models/NoteViewerUsers'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(NoteViewerUsers, ({ faker }) => {
  return {
    //
  }
}).build()
