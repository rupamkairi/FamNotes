import BaseSeeder from '@ioc:Adonis/Lucid/Seeder'

const editonlyNotesUsersMapping = [{ note_id: 1, user_id: 3 }]

export default class extends BaseSeeder {
  public async run() {
    // editonly Model Required for seeding
  }
}
